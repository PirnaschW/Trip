@model IEnumerable<Trip.Models.DestinationDisp>

@{
  ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
  @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
  <tr>
    <th>
      @Html.DisplayNameFor(model => model.Name)
    </th>
    <th>
      @Html.DisplayNameFor(model => model.Region.Name)
    </th>
    <th>
      @Html.DisplayNameFor(model => model.NatureRating)
    </th>
    <th>
      @Html.DisplayNameFor(model => model.NightlifeRating)
    </th>
    <th>
      @Html.DisplayNameFor(model => model.ShoppingRating)
    </th>
    <th></th>
  </tr>

  @foreach (var item in Model)
  {
    <tr>
      <td>
        @Html.DisplayFor(modelItem => item.Name)
      </td>
      <td>
        @Html.DisplayFor(modelItem => item.Region.Name)
      </td>
      <td>
        @Html.DisplayFor(modelItem => item.NatureRating)
      </td>
      <td>
        @Html.DisplayFor(modelItem => item.NightlifeRating)
      </td>
      <td>
        @Html.DisplayFor(modelItem => item.ShoppingRating)
      </td>
      <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.DestinationID }) |
        @Html.ActionLink("Details", "Details", new { id = item.DestinationID }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.DestinationID })
      </td>
    </tr>
  }

</table>
